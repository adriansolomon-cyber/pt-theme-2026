<?php
/**
 * Product IDs for the price audit rendered by templates/page-test.php.
 *
 * Return a flat array of product IDs (integers). The test page paginates these
 * 50 at a time on screen and can export the full per-sale detail as CSV.
 *
 * Replace the list below with the full 800+ set. One ID per line keeps diffs
 * readable; trailing commas are fine. Non-numeric junk is ignored by the loader.
 *
 * @package pt-theme-2026
 */

defined( 'ABSPATH' ) || exit;

return array(
	// --- Seed set (the original 22) — replace with the full list. ---
	16261, 22040, 18432, 102075, 111948, 14795, 122926, 56961, 58078,
	63159, 67573, 16048, 17292, 102162, 16616, 69686, 18610, 16553,
	45308, 46710, 49119, 77662,
);
